# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty — no source files, build configuration, or documentation have been committed yet. There is no codebase to derive build/test commands or architecture from.

When code is added, this file should be regenerated to cover:

- **Commands**: install, build, lint, test (including how to run a single test), and any project-specific dev workflows.
- **Architecture**: the big-picture structure that requires reading multiple files to understand — module boundaries, data flow, key abstractions, and external integrations.
- **Conventions**: anything captured in `README.md`, `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md` once those exist.

Until then, ask the user for context (stack, intended scope of the "SMA tool", entry points) before writing code.
